---
description: Java code style and optimization guidelines
globs: *.java
alwaysApply: false
---
# Java Best Practices

1. 不要向下和向后兼容
2. 不要使用全限定类名
3. braces 不要使用 K&R style，使用 Allman style
4. 最好使用显示类型声明，而不是隐式类型声明
5. 最好不要编写内部类，而是拆分文件写
6. 字符串拼接不要使用 + 号，最好使用 Template 或者 文本块 方式拼接，例如:

```java
String toolName = "read_file";
// ❌ 错误示例:
String result = "Hello, " + name + "!";
String xml = "<"
                        + toolName
                        + ">\n"
                        + "<path>doc.txt</path>\n"
                        + "</"
                        + toolName
                        + ">";
// ✅ 正确示例1:
String xml = """
<%s>
<path>doc.txt</path>
</%s>
""".formatted(toolName);
// ✅ 正确示例2:
String msg = MessageFormat.format("<{0}> <path>doc.txt</path> </{0}>", toolName);
```
